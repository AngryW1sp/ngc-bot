
services:
  bot:
    image: angryw1sp/ngc-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/data/players.db    # путь внутри контейнера
    volumes:
      - ./data:/data   